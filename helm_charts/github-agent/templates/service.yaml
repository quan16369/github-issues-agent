apiVersion: v1
kind: Service
metadata:
  name: {{ include "github-agent.fullname" . }}
  labels:
    {{- include "github-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "github-agent.selectorLabels" . | nindent 4 }}
